// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model User {
  id          String    @id @default(cuid())
  name        String    @default("")
  email       String    @unique @default("")
  enabled     Boolean   @default(true)
  createdDate DateTime? @default(now())
  password    String
  projects    Project[] @relation("Project_users")
  teams       Team[]    @relation("Team_members")
}

model Project {
  id          String    @id @default(cuid())
  name        String    @default("")
  description String    @default("")
  createdDate DateTime? @default(now())
  users       User[]    @relation("Project_users")
  teams       Team[]    @relation("Project_teams")
  samples     Sample[]  @relation("Project_samples")
}

model Sample {
  id       String    @id @default(cuid())
  name     String    @default("")
  organism String?
  projects Project[] @relation("Project_samples")
}

model Team {
  id                   String    @id @default(cuid())
  name                 String    @default("")
  description          String    @default("")
  members              User[]    @relation("Team_members")
  parentTeam           Team?     @relation("Team_parentTeam", fields: [parentTeamId], references: [id])
  parentTeamId         String?   @map("parentTeam")
  from_Project_teams   Project[] @relation("Project_teams")
  from_Team_parentTeam Team[]    @relation("Team_parentTeam")

  @@index([parentTeamId])
}